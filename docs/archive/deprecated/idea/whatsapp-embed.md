# WhatsApp 嵌入技术实践

> 状态：规划中
> 创建：2024-02-04

## 核心问题

**用户期望的交互：** 点击浮动图标 → 展开对话窗口 → 在网页内聊天

**技术现实：** WhatsApp 官方不提供网页内嵌聊天 SDK，只提供跳转方案

---

## WhatsApp 官方方案（暂不采纳）

> **结论：官方只提供跳转链接，不提供网页内嵌聊天能力，无法满足"点击展开对话窗口"的交互需求。**

官方文档：
- [Click to Chat](https://faq.whatsapp.com/5913398998672934)
- [QR Codes & Short Links API](https://developers.facebook.com/docs/whatsapp/overview/qr-codes)
- [WhatsApp Business Developer Hub](https://business.whatsapp.com/developers/developer-hub)

<details>
<summary>官方方案详情（点击展开）</summary>

### 方案 1：Click-to-Chat 链接（基础）

**URL 格式：**
```
https://wa.me/<phone>?text=<urlencodedtext>
```

**电话号码规则：**
- 使用国际格式，不含 `+`、`-`、`()`、空格
- 示例：`https://wa.me/8613800138000`（中国号码）

**优点：** 零成本、零配置、立即可用
**缺点：** 暴露手机号、预填消息写死在 URL 中

---

### 方案 2：QR Codes & Short Links API

**创建短链接：**
```bash
curl 'https://graph.facebook.com/v24.0/{phone-number-id}/message_qrdls' \
  -H 'Authorization: Bearer {access_token}' \
  -d '{"prefilled_message": "Hi", "generate_qr_image": "SVG"}'
```

**返回：** `https://wa.me/message/XXXX`（隐藏手机号）

**优点：** 隐藏号码、可改预填消息、可生成 QR 码
**缺点：** 需要 WhatsApp Business API 接入 + Meta 商业验证

---

### 官方能力边界

| 功能 | 官方支持 |
|------|----------|
| 网页内嵌聊天窗口 | ❌ |
| 网页内收发消息 | ❌ |
| 自定义聊天 UI | ❌ |

</details>

---

## 技术方案对比

### 方案 A：Click-to-Chat（标准方案）

```
用户点击图标 → 展开预览窗口 → 点击发送 → 跳转 WhatsApp App/Web
```

**实现：**
```typescript
// WhatsApp Click-to-Chat URL
const waUrl = `https://wa.me/${phone}?text=${encodeURIComponent(prefilledMessage)}`;
```

**优点：**
- 零成本，无后端
- 无需 Meta 商业验证
- 加载性能最优

**缺点：**
- 必须跳转离开网站
- 无法在网页内完成对话

---

### 方案 B：伪对话窗口 + Click-to-Chat

```
用户点击图标 → 展开聊天窗口 UI → 输入/选择消息 → 点击发送 → 跳转 WhatsApp
```

**这是你想要的交互形式**，技术上完全可行：

1. 浮动图标（自己实现或用轻量库）
2. 展开一个聊天窗口 UI（纯前端组件）
3. 窗口内可以：
   - 显示欢迎消息/头像
   - 提供预设问题按钮
   - 让用户输入自定义消息
4. 点击"发送"时拼接 `wa.me` 链接跳转

**优点：**
- 交互体验接近真实聊天
- 可收集用户意图（选了哪个预设问题）
- 仍然零后端成本

**缺点：**
- 最终仍需跳转
- 用户可能期望是真聊天，产生落差

---

### 方案 C：WhatsApp Business API + 第三方平台

```
用户点击图标 → 展开真实聊天窗口 → 在网页内收发消息
```

**需要：**
- Meta 商业账号验证
- 第三方平台（Chatwoot、Callbell、Trengo、Interakt 等）
- 月费 $15-100+
- 后端 Webhook 接收消息

**优点：**
- 真正的网页内聊天
- 多客服支持
- 消息记录、自动化

**缺点：**
- 成本高
- 技术复杂度高
- 对于询盘量不大的 B2B 网站可能过度

---

## 推荐方案

**方案 B：伪对话窗口 + Click-to-Chat**

理由：
1. 符合交互预期（点击展开窗口）
2. 零后端成本
3. 技术简单，可自己实现
4. 跳转到 WhatsApp 对 B2B 用户是可接受的（他们习惯用 WhatsApp 沟通）

---

## 实现细节

### 组件结构

```
<WhatsAppWidget>
  ├── <FloatingButton />      // 浮动图标，右下角
  └── <ChatWindow />          // 展开的聊天窗口
       ├── <Header />         // 头像 + 名称 + 在线状态
       ├── <WelcomeMessage /> // 欢迎语气泡
       ├── <QuickReplies />   // 预设问题按钮
       ├── <InputField />     // 自定义消息输入
       └── <SendButton />     // 触发跳转
```

### 交互流程

1. **页面加载**：显示浮动 WhatsApp 图标
2. **点击图标**：展开聊天窗口（动画）
3. **窗口内容**：
   - "Hi! 👋 How can we help you?"
   - 预设按钮：`Request Quote` / `Product Inquiry` / `Technical Support`
   - 或自定义输入框
4. **点击发送**：`window.open(waUrl, '_blank')`

### 性能考虑

- 组件懒加载（用户首次点击时加载窗口）
- 图标用 SVG inline，不依赖外部资源
- 不使用第三方 SDK，纯自研组件

### 位置与 Cookie 弹窗协调

参考 Idea 2，确保：
- WhatsApp 图标在右下角
- Cookie 弹窗也在右下角但更高
- 两者不重叠

---

## 开源参考

### React 组件对比

| 库 | Stars | 最近更新 | TypeScript | 聊天窗口 | 深色模式 | 推荐度 |
|----|-------|----------|------------|----------|----------|--------|
| [react-floating-whatsapp](https://github.com/awran5/react-floating-whatsapp) | 91 | 2022-12 | ✅ | ✅ 完整 | ✅ | ⭐⭐⭐ |
| [@carlos8a/react-whatsapp-floating-button](https://github.com/CarlosUlisesOchoa/react-whatsapp-floating-button) | 14 | 2024-10 | ✅ | ✅ 完整 | ✅ | ⭐⭐⭐⭐ |
| [react-whatsapp-widget](https://github.com/ann0nip/react-whatsapp-widget) | 45 | 2023-03 | ❌ | ✅ 完整 | ❌ | ⭐⭐ |

### 详细分析

#### 1. @carlos8a/react-whatsapp-floating-button （推荐）

```bash
npm install @carlos8a/react-whatsapp-floating-button
```

**优点：**
- 2024 年 10 月还在更新，维护活跃
- 完整 TypeScript 支持
- 内置深色/浅色模式
- 28+ 可配置 props
- Vite 构建，体积小

**Demo:** 模拟 WhatsApp 聊天界面，用户输入后跳转 WhatsApp

**适配项目：** Next.js + TypeScript ✅

---

#### 2. react-floating-whatsapp

```bash
npm install react-floating-whatsapp
```

**优点：**
- Stars 最多（91），社区验证
- TypeScript 支持
- 通知音效 + 循环提醒
- Storybook 文档完善

**缺点：**
- 2022 年后未更新
- 可能存在依赖过时问题

**Demo:** https://react-floating-whatsapp.vercel.app/

---

#### 3. react-whatsapp-widget

```bash
npm install react-whatsapp-widget
```

**优点：**
- 界面简洁
- 可自定义 Icon

**缺点：**
- 无 TypeScript
- 无深色模式
- 更新停滞

**Demo:** https://react-whatsapp-widget-4d84a1.netlify.app

---

### Vanilla JS 方案（备选）

| 库 | 依赖 | 特点 |
|----|------|------|
| [rafaelbotazini/floating-whatsapp](https://github.com/rafaelbotazini/floating-whatsapp) | jQuery | 经典方案，需要 jQuery |
| [liveupx/Floating-Calls-Social-Buttons](https://github.com/liveupx/Floating-Calls-Social-Buttons-for-Website-free) | 无 | 多平台支持（WhatsApp/Telegram/微信等），~5KB |

---

## 推荐实现路径

### 选项 1：直接使用 @carlos8a/react-whatsapp-floating-button

**适合场景：** 快速上线，功能够用

```tsx
import { WhatsAppWidget } from '@carlos8a/react-whatsapp-floating-button';

<WhatsAppWidget
  phoneNumber="8613800138000"
  accountName="Tianze Support"
  statusMessage="Typically replies within 1 hour"
  initialMessageByServer="Hi! How can we help you today?"
  darkMode={false}
/>
```

**需验证：**
- [ ] Next.js App Router 兼容性（Client Component）
- [ ] 包体积影响
- [ ] 样式与项目设计系统冲突

---

### 选项 2：自研组件（参考开源实现）

**适合场景：** 需要完全控制样式、功能定制

**参考 UI：** 上述开源库的聊天窗口设计
**核心逻辑：** 自己实现，避免外部依赖

```
优点：零依赖、完全可控、可与 Idea 1（对话式表单）复用 UI
缺点：开发时间
```

---

## 下一步行动

1. [ ] 测试 @carlos8a/react-whatsapp-floating-button 在项目中的兼容性
2. [ ] 评估包体积（bundle analyzer）
3. [ ] 确认样式定制能力是否满足需求
4. [ ] 决定：直接用 vs 自研

---

## 待决策

1. [ ] 预设问题内容（需要哪些快捷回复选项）
2. [ ] 是否需要多语言支持（跟随网站 locale）
3. [ ] 是否显示"在线状态"（可以按营业时间模拟）
4. [ ] 移动端是否直接打开 WhatsApp App（跳过窗口）

---

## 路径演进：网页内真实聊天（消除跳转）

> 添加日期：2026-02-26
> 背景：方案 B 已实现并上线，但最终仍需跳转到 WhatsApp Web/App，体验不友好。
> 项目现状：已有 WhatsApp Business API 后端（`/api/whatsapp/send` + `/api/whatsapp/webhook`）。

### 问题

当前方案 B 的用户体验断裂点：

```
用户在网页输入消息 → 点击发送 → 跳转离开网站 → 打开 WhatsApp → 继续对话
```

对海外买家来说，跳转意味着离开产品页面，增加流失风险。

### 方案 D：开源客服系统集成（推荐评估）

用开源自托管客服平台替代自建实时聊天，直接获得「网页内收发 WhatsApp 消息」能力。

#### 候选方案

| 平台 | 类型 | WhatsApp 集成 | 网页 Widget | 自托管 | 许可证 |
|------|------|---------------|-------------|--------|--------|
| **Chatwoot** | 全功能客服 | WhatsApp Cloud API 原生支持 | 内置 | Docker | MIT |
| **Typebot** | 对话式表单 | 通过 Webhook | 内置 | Docker | AGPL-3.0 |
| **Rocket.Chat** | 团队通讯+客服 | 通过 App | 内置 | Docker | MIT |

#### Chatwoot 方案详情（首选）

**架构：**

```
买家在网页聊天 Widget 输入
  → Chatwoot 服务端接收
  → 通过 WhatsApp Cloud API 发送到销售的 WhatsApp
  → 销售在 WhatsApp App 或 Chatwoot 后台回复
  → Chatwoot 推送到网页 Widget 实时显示
```

**优势：**
- 网页端真实双向聊天，无需跳转
- 多渠道统一（WhatsApp + 网页 + Email）
- 多客服分配、自动回复、对话标签
- 访客信息自动采集（页面来源、地区）
- 开源自托管，数据完全可控
- Docker 一键部署

**成本评估：**
- 自托管：服务器费用（约 $5-20/月 VPS）
- WhatsApp Business API：Meta 按对话收费（前 1000 条/月免费）
- 人力：初始部署 + 持续维护

**需要验证：**
- [ ] Chatwoot Docker 部署复杂度
- [ ] WhatsApp Cloud API 对接流程（需 Meta 商业验证）
- [ ] 网页 Widget 样式自定义程度（能否适配项目设计系统）
- [ ] 性能影响（Widget JS 体积、加载策略）
- [ ] 是否支持多语言 Widget（跟随网站 locale）

#### 与方案 B 的关系

方案 B（当前）和方案 D 可以共存：
- **短期**：保持方案 B 作为降级方案
- **中期**：部署 Chatwoot，接入 WhatsApp Cloud API
- **切换**：验证 Chatwoot 稳定后，替换前端 Widget

#### 排除方案：vercel/chat

> 评估日期：2026-02-26

vercel/chat（GitHub: vercel/chat）是 Vercel 出品的统一 chatbot SDK，支持 Slack/Teams/Discord 等平台。**不适用于本项目**：
1. 不支持 WhatsApp — 无 WhatsApp adapter
2. 解决的是不同问题 — 服务端 bot SDK，非前端客服 Widget
3. 面向内部团队通讯，非面向客户的网页嵌入聊天

---

## 参考资料

- [Elfsight: How to Add Floating WhatsApp Button](https://elfsight.com/blog/how-to-embed-floating-whatsapp-button-to-website/)
- [WhatsApp Business Developer Hub](https://business.whatsapp.com/developers/developer-hub)
- [GitHub: floating-whatsapp](https://github.com/rafaelbotazini/floating-whatsapp)
