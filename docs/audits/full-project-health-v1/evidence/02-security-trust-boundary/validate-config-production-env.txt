
> b2b-web-template@0.1.0 validate:config /Users/Data/conductor/workspaces/tianze-website/miami
> tsx scripts/validate-production-config.ts

❌ Errors:
  - SITE_CONFIG.baseUrl is not configured for production: http://localhost:3000
  - Production rate limiting and idempotency require Upstash Redis or Vercel KV. Configure at least one store.
  - RATE_LIMIT_PEPPER is required (production rate-limit keys rely on it and runtime already throws when it is missing or weak).
  - NEXT_SERVER_ACTIONS_ENCRYPTION_KEY is required (Server Actions deployment requires a stable encryption key).
  - TURNSTILE_SECRET_KEY is required (Contact form verification depends on server-side Turnstile validation).
  - NEXT_PUBLIC_TURNSTILE_SITE_KEY is required (the live Contact form widget depends on a public Turnstile site key).
  - RESEND_API_KEY is required (the shipped lead pipeline sends admin notification email through Resend).
  - AIRTABLE_API_KEY is required (the shipped lead pipeline persists lead records in Airtable).
  - AIRTABLE_BASE_ID is required (the shipped lead pipeline persists lead records in Airtable).
 ELIFECYCLE  Command failed with exit code 1.
exit=1
